# AI对话生成分组模板 PRD

## 1. 目标
- 通过中文自然语言对话，引导用户生成 1 个分组模板 XML 草稿。
- 用户可在可视化区域直接编辑分组树和参数，最终手动保存。
- 系统需具备上下文理解能力，支持增量修改已有分组结构。

## 2. 范围
- 每次会话仅新建 1 个模板文件。
- 暂不支持修改历史文件。
- 输出为草稿模板，允许后续人工补全。
- 仅中文交互。

## 3. 页面结构
- 左侧：AI 对话区。
- 右侧：分组树预览区 + 参数卡片区 + XML 内容区。
- 首屏展示固定样例，并告诉用户如何描述需求。

## 4. 参数展示与编辑
- 系统先展示“支持的零件级参数全量列表框”。
- 用户通过自然语言选择/描述零件级参数。
- 右侧展示并可编辑两类卡片：
1. 零件级参数卡片（Part 参数）
2. 分组级参数卡片（当前选中 Group 节点参数）

## 5. 分组树能力
- AI 自动生成初始分组树（可多层嵌套）。
- 用户可在右侧执行：
1. 新增分组节点
2. 删除分组节点
3. 拖拽调整层级
4. 编辑分组参数

## 6. 自动填充规则
- 用户主要描述零件级参数。
- 系统自动为分组级参数填默认值。
- 用户有特别指定时，覆盖默认值。
- `特征选择` 支持自然语言匹配特征词典：
1. 先告知用户可选特征
2. 自动匹配并写入
3. 低置信度时追问澄清

## 7. 上下文理解（核心）
- 系统必须基于当前分组树做增量编辑，不是每轮重建。
- 典型规则：
1. 指代复用：`也一样/同理/同上` 复用上一条结构操作
2. 批量应用：`A侧和B侧都...` 对多个目标同时执行
3. 相对定位：`在X下面新增Y` 精确插入指定父节点
4. 去重：节点已存在时不重复创建，并给出提示

示例：
- 已有同级分组 `A侧`、`B侧`
- 用户输入：`在A侧还需要添加外圆和端面2个子分组，B侧也一样`
- 结果：在 `A侧`、`B侧` 下各新增 `外圆`、`端面`

## 8. 定义层编辑规则
- 允许用户编辑 `Part_Template` 和 `Group_Template`。
- 新增定义参数：自动补齐到所有对应实例节点，默认空值。
- 删除定义参数：自动从所有对应实例节点同步删除。

## 9. XML 与校验
- 右侧实时展示当前 XML 内容。
- 保存前仅做基础校验：
1. XML 结构可解析
2. 必须包含 `Part_Template`、`Group_Template`、`Part`

## 10. 保存规则
- 默认不自动落盘。
- 用户点击保存时写文件到 `分组模板/`。
- 文件名自动生成：`分组模板_<时间戳>.xml`。

## 11. 非目标（当前阶段）
- 账号体系与权限管理
- 历史模板修改流程
- 完整发布门禁校验

## 12. 验收标准
1. 用户能通过自然语言生成 1 个草稿模板
2. 支持多层嵌套分组创建与编辑
3. 支持上下文增量指令（含“也一样”类指代）
4. 右侧卡片支持 Part 与 Group 参数编辑
5. 点击保存后成功生成符合基础结构的 XML 文件

