# 分组模板文件结构说明

## 1. 文档目的
本说明用于定义当前项目中“分组模板 XML”的标准结构和约束，作为后续系统设计与开发的输入。

## 2. 目录与文件角色
- 特征词典目录：`特征选择列表/`
- 特征词典文件：`FeatureTemplate.xml`
- 分组模板目录：`分组模板/`
- 分组模板文件：每个 XML 为一个用户样例模板

## 3. XML 总体结构
每个分组模板 XML 根节点为 `<Kmsoft>`，内部按顺序包含三类节点：

1. `Item type="Part_Template"`  
定义零件节点（Part）允许填写的参数（参数定义层）。

2. `Item type="Group_Template"`  
定义分组节点（Group）允许填写的参数（参数定义层）。

3. `Item type="Part"`  
具体零件实例，内部包含：
- `<Params>`：填写 Part 参数值（应与 `Part_Template` 对应）
- 若干 `Item type="Group"`：分组树结构（可嵌套），每个 Group 内含 `<Params>`（应与 `Group_Template` 对应）

## 4. 参数定义层与实例层关系
### 4.1 Part_Template -> Part.Params
- `Part_Template` 中 `GridProp/Item name="..."` 定义参数名与类型。
- `Part` 节点下 `<Params><param name="..." value="..."/></Params>` 填写对应参数值。
- 原则：`Part.Params` 参数名应来自 `Part_Template` 定义。

### 4.2 Group_Template -> Group.Params
- `Group_Template` 中 `GridProp/Item name="..."` 定义分组参数名与类型。
- 每个 `Group` 节点下 `<Params><param name="..." value="..."/></Params>` 填写对应参数值。
- 原则：`Group.Params` 参数名应来自 `Group_Template` 定义。
- 说明：`名称`为分组实例标识，通常存在于 `Group.Params`，可作为系统保留实例字段。

## 5. 当前模板中常见参数
### 5.1 零件级（Part）常见参数
- 方向类：`主方向`、`主方向1..6`
- 基准类：`原点`
- 轴线类：`有主轴线`、`有轴线`
- 工艺开关类：`是否需要加工面分离`
- 自动判定类：`是否自动按坐标轴方向加工面积多少选择主方向`
- 车削相关：`需要判断外圆车削加工方向`、`需要判断多外圆车削加工方向`

### 5.2 分组级（Group）常见参数
- 依赖关系：`依赖方向`、`依赖方式`
- 特征：`特征选择`
- 轴线判断：`主轴线上特征`、`一般轴线上特征`
- 其他：`是否按用户规则排工序`
- 实例标识：`名称`

## 6. 特征选择约束（关键）
`Group` 中 `param name="特征选择"` 的值必须来源于特征词典文件：

- 来源文件：`特征选择列表/FeatureTemplate.xml`
- 来源路径：`FeatureSelect` 下所有 `Item name="..."`（含分类项与具体项，系统可按配置决定是否仅允许叶子项）
- 多值分隔：当前样例使用英文逗号 `,`

建议系统规则：
1. 保存前拆分校验每个特征 token。
2. 非词典项一律判错（例如占位文案“请双击进行选择”）。
3. 空值允许与否可按业务场景配置（草稿允许，发布不允许）。

## 7. 方向与依赖关系建议规则
基于现有模板习惯，系统可内置以下默认映射：

1. 当 Part 定义了 `主方向` 或 `主方向1..6`：
- Group `依赖方向` 的可选项应动态包含：`从父`、所有主方向项、`任意方向`。

2. 一级方向组（如“方向1”）默认：
- `依赖方向 = 主方向N`
- `依赖方式 = 相同`

3. 子组默认：
- `依赖方向 = 从父`

## 8. 建议的数据模型（系统实现）
可拆成四层对象：

1. `FeatureDictionary`
- `name`
- `children[]`

2. `PartTemplateField`
- `name`
- `type`
- `defaultValue`
- `visible/editable`
- `dataType/resPath`

3. `GroupTemplateField`
- `name`
- `type`
- `defaultValue`
- `visible/editable`
- `dataType/resPath`

4. `PartInstance / GroupInstance`
- `id`
- `params`（键值）
- `children`（仅 Group 可嵌套）

## 9. 保存校验清单（建议直接落地）
### 9.1 结构校验
- 必须存在且顺序合法：`Part_Template`、`Group_Template`、`Part`
- 根节点必须为 `Kmsoft`

### 9.2 参数校验
- `Part.Params` 参数必须在 `Part_Template` 定义中（可允许白名单扩展，如 `名称`）
- `Group.Params` 参数必须在 `Group_Template` 定义中（可允许 `名称`）

### 9.3 取值校验
- `特征选择` 必须来自特征词典
- `依赖方向/依赖方式` 必须落在其枚举定义中
- 方向引用一致性：若填 `主方向N`，Part 必须定义该方向参数

### 9.4 工程质量校验
- XML 编码声明与实际编码一致（建议统一 UTF-8）
- XML 语法完整可解析

## 10. 推荐系统功能模块
1. 模板解析器（XML -> 内存模型）
2. 模板编辑器（定义层 + 实例层）
3. 词典管理器（特征词典维护）
4. 校验引擎（结构/参数/词典/枚举）
5. 发布与版本管理（草稿、发布、回滚、差异对比）
6. 导入导出（批量 XML）

## 11. 后续实现建议
先实现“只读解析 + 校验报告”，再做“可编辑 + 自动补全”：

1. 第一步：全量解析现有模板并输出错误清单。
2. 第二步：实现参数联动（Part 变更驱动 Group 可选项更新）。
3. 第三步：实现发布门禁（未通过校验不可发布）。

