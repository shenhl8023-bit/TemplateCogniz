# 零件参数与分组参数关系解析及数据字典

本文档基于 `分组模板/` 目录下的所有真实 XML 配置提取汇总，旨在详细说明整个系统中“零件（Part）”与“分组（Group）”级参数的层级关系、相互依赖逻辑，以及各个参数的数据字典规范。

## 一、 核心架构：参数间的依赖绑定关系
零件级参数和分组级参数之间不仅存在自顶向下的作用域关联，更存在着强烈的**“定义与引用”（类似代码中的变量声明与调用）**的关系。

零件级参数负责“建立全工件全局的坐标系和宏观策略”，而分组级参数负责“引用这些基准宏观策略，进行具体特征集合的筛选与匹配”。

### 1. 【主方向绑定】（箱体/结构件的法向加工逻辑）
*   **【零件级】定义锚点：** `主方向1` ~ `主方向6`。由工艺人员（或CAD系统）指定该零件在三维空间中的最多 6 个主要加工面的法向量方向。
*   **【分组级】定向引用：** 分组通过 `依赖方向` 参数的下拉列表调用这些法向基准。当某个分组的 `依赖方向` 设置为“主方向1”时，系统会在执行该组的特征提取规则时，去提取零件中预设的“主方向1”的具体 XYZ 矢量坐标，作为组内几何特征方向比对的靶向基准。

### 2. 【车削环境关联】（回转体/车削件宏观切换）
*   **【零件级】开启车削标识：** `需要判断外圆车削加工方向` / `需要判断多外圆车削加工方向`。这两个开关决定了系统是否进入车削面判断策略。
*   **【分组级】专属引用模式：** 只有在前置条件开启时，分组的 `依赖方向` 才可能被有效地设置为针对车削特化的类型，如“外圆加工方向”。这圈定了该组只接纳属于车床加工范围的旋转面特征。

### 3. 【中心轴线深度绑定】
*   **【零件级】轴线先决条件：** `有主轴线` / `有轴线`。从零件宏观定性，确认模型是否为一个具有主中心轴的回转件。
*   **【分组级】空间过滤：** 结合零件的宏观属性，分组利用 `主轴线上特征` / `一般轴线上特征` 等过滤器，精准剥离出“纯心部位属性特征”（如中心通直孔），还是“偏心/侧边属性特征”。

---

## 二、 零件级参数 (Part Parameters) 数据字典
零件级参数通常在 XML `<Item type="Part_Template">` 中声明。

| 参数名称 | 数据类型 | 常见可选值 / 默认值 | 作用解析 |
| :--- | :--- | :--- | :--- |
| **原点** | String (cad) / Point | 自定义 `(X,Y,Z)` | 零件的绝对零点基准。定义三维加工坐标系 `0,0,0` 位置。 |
| **主方向** (以及 `主方向1~6`) | String (direction) | 自定义矢量输入 | 定义零件用于加工时最多允许的个核心方向面。 |
| **是否自动按坐标轴方向加工面积多少选择主方向**| Multi (单选) | `是` (默认) / `否` | 决定是否由系统面积算法自动接管并计算主要加工方向优先级。 |
| **是否需要加工面分离**| Multi (单选) | `是` (默认) / `否` | 对于复杂特征拓扑或不连续面，是否在处理前做几何连通面分离提取。 |
| **有主轴线** | Multi (单选) | `是` (默认)<br>`不是`<br>`否` | 对回转类零件（如衬套、活门）特有的轴心判断。 |
| **有轴线** | Multi (单选) | `是` (默认) / `不是` | 同上。 |
| **需要判断外圆车削加工方向**|  Multi (单选) | `是` (默认) / `不是` | 针对车削加工工序方向判定的整体开关。 |
| **需要判断多外圆车削加工方向**| Multi (单选) | `是` (默认) / `不是` | 等同上。 |

---

## 三、 分组级参数 (Group Parameters) 数据字典
分组级参数通常在 XML `<Item type="Group_Template">` 中声明。

| 参数名称 | 数据类型 | 常见可选字典项 (Multi) | 作用解析 |
| :--- | :--- | :--- | :--- |
| **依赖方向** | Multi (单选) | `任意方向` (默认)<br>`从父`<br>`主方向1~6`<br>`外圆加工方向`<br>`多外圆加工方向`<br>`六面方向`<br>`无可行加工方向`<br>`未配置` | **核心基准源头**。它通过提取上面列表里的字符串选项，映射到之前定义的全局“零件参数（基准标）”，决定当前特征组所锚定的目标朝向。 |
| **依赖方式** | Multi (单选) | `无` 或空值 (默认)<br>`相同`<br>`相反`<br>`平行`<br>`平行且在同侧`<br>`平行且在反侧`<br>`垂直`<br>`不平行`<br>`接近`<br>`接近反向`<br>`相同或接近`<br>`相反或接近反向`<br>`与坐标轴方向不平行` | **集合筛选算法**。特征的真实方向与上面选定好的“依赖方向”比对，满足这里的拓扑/几何约束才会通过筛选并归入该组。 |
| **特征选择** | String (kmfeatype) | 从 `kmfeatype` 库动态加载。例如：`平面`, `孔`, `台阶`, `单纯底凹槽`, `矩形底凹槽`, `螺纹孔` 等等 | 字符串，支持多选（逗号拼接）。限定本组物理意义上包含哪一类的零件特征模型。 |
| **主轴线上特征** | Multi (单选) | `无关` (默认)<br>`是`<br>`不是` | 用于圆周体：强制通过几何属性判断特征是否居于中心主轴（孔/外圆特征）。 |
| **一般轴线上特征** | Multi (单选) | `无关` (默认)<br>`是`<br>`不是` | 类似上面，但放松阈值针对通常轴线。 |
| **是否按用户规则排工序** | Multi (单选) | `否` (默认)<br>`是` | 用来指定加工排序和工步编排的归属策略。 |
